{
  "name": "pi-pp-platform",
  "private": true,
  "engines": {
    "node": ">=22.19.0"
  },
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "parity": "node parity/audit.mjs"
  },
  "devDependencies": {
    "yaml": "^2.4.0"
  }
}
